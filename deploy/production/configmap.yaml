# Cluster-internal config map. Real secrets (CONNECTION_VAULT_KEY,
# APP_SECRET_KEY, DATABASE_URL with creds) live in `x402guard-secrets`,
# which is provisioned out-of-band — `kubectl create secret generic
# x402guard-secrets --from-env-file=secrets.env` from a private repo or
# a sealed-secrets controller.
apiVersion: v1
kind: ConfigMap
metadata:
  name: x402guard-config
  namespace: x402guard
data:
  SOLANA_RPC_URL: https://api.mainnet-beta.solana.com
  AGENT_VAULT_PROGRAM_ID: 5s9rscxcoXZMLwn2cenGYhj6zd5voyMHAmFBe4qhZQxH
  USDC_MINT: EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
  CORS_ALLOWED_ORIGINS: https://x402guard.acedata.cloud
