apiVersion: apps/v1
kind: Deployment
metadata:
  name: x402guard-web
  namespace: acedatacloud
  labels:
    app: x402guard
    component: web
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: x402guard
      component: web
  template:
    metadata:
      labels:
        app: x402guard
        component: web
    spec:
      imagePullSecrets:
        - name: docker-registry
      containers:
        - name: web
          image: ghcr.io/acedatacloud/x402guard-web:${TAG}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /index.html
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /index.html
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 40m
              memory: 48Mi
            limits:
              cpu: 200m
              memory: 256Mi

---
apiVersion: v1
kind: Service
metadata:
  name: x402guard-web
  namespace: acedatacloud
  labels:
    app: x402guard
    component: web
spec:
  type: ClusterIP
  selector:
    app: x402guard
    component: web
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
